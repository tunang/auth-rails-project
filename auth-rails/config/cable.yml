development:
  adapter: redis
  url: redis://localhost:56379/1

test:
  adapter: async

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:56379/1" } %>
  channel_prefix: myapp_production
